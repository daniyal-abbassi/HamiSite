{
  "$comment": "Worker manifest. Every flag below was read out of the local `--help` on 2026-09-25 22:3x +0330 — not from documentation and not guessed. Fields named *_verified carry the command that proved them.",
  "generated": "2026-09-25",
  "workers": {
    "qoder": {
      "id": "qoder",
      "command": "/home/lain/.local/bin/qodercli",
      "command_verified": "ps -eo cmd showed pid 4416 running /home/lain/.local/bin/qodercli; `qoder --version` -> 1.1.63",
      "version": "1.1.63",
      "entry_shim": "/home/lain/.qoder/entry/qoder",
      "default_mode": "headless",
      "non_interactive_flag": ["-p", "--print"],
      "cwd_flag": ["-w", "<dir>"],
      "output_format_flag": ["-o", "--output-format", "<format>"],
      "output_format_values": "UNVERIFIED — --help names the flag but not its choices; the dispatcher probes once and records the accepted value",
      "permission_flag": ["--permission-mode", "<default|accept_edits|bypass_permissions|dont_ask|auto>"],
      "tool_restriction_flags": ["--tools <tools...>", "--allowed-tools <tool>", "--disallowed-tools <tool>"],
      "model_flag": ["-m", "<model>", "--list-models"],
      "budget_flags": ["--max-output-tokens <size>", "--thinking-budget <tokens>", "--context-window <size>"],
      "supports_resume": true,
      "resume_flags": ["-c/--continue", "-r/--resume [id]", "--session-id <id>", "-n/--name <name>", "--fork-session"],
      "supports_stdin": false,
      "prompt_transport": "positional query argument",
      "supports_worktree": true,
      "worktree_flag": ["--worktree", "[name]"],
      "supports_json": "probe",
      "enabled": true,
      "never_use": ["--dangerously-skip-permissions"],
      "roles": ["implementation", "repository-heavy", "spec-aligned work", "review"]
    },
    "hermes": {
      "id": "hermes",
      "command": "/home/lain/.local/bin/hermes",
      "command_verified": "ps -eo cmd showed pid 17028 = /home/lain/.hermes/hermes-agent/venv/bin/python3 .../bin/hermes; the launcher is on disk at ~/.local/bin/hermes (not on this session's PATH)",
      "version": "UNVERIFIED — `hermes --version` was not run blind; `hermes version` is a subcommand",
      "default_mode": "one-shot",
      "non_interactive_flag": ["-z", "--oneshot", "<prompt>"],
      "oneshot_verified": "--help usage line: '[-z PROMPT]' and '-z PROMPT, --oneshot PROMPT'; help states it applies to -z/--oneshot and --tui",
      "cwd_flag": "NONE — no --cwd in --help; the dispatcher sets the process working directory",
      "output_format_flag": "NONE in --help",
      "permission_flag": "NONE — --yolo exists (auto-approve) and is refused by the dispatcher; --safe-mode and --ignore-rules exist but their semantics were not verified",
      "tool_restriction_flags": ["-t/--toolsets <comma-list>"],
      "toolsets_note": "'Built-in toolsets use plain names (e.g. web, memory). MCP tools use server:tool notation.' `hermes tools list` refuses to run non-interactively ('requires an interactive terminal'), so the vocabulary CANNOT be enumerated from the dispatcher. -t is therefore opt-in via --toolsets, never assumed.",
      "model_flag": ["-m/--provider/--reasoning-effort? (-m MODEL, --provider PROVIDER verified)"],
      "budget_flags": [],
      "supports_resume": true,
      "resume_flags": ["--resume <SESSION id or title>", "--continue [SESSION_NAME]", "--pass-session-id"],
      "supports_stdin": false,
      "prompt_transport": "-z argument",
      "supports_worktree": true,
      "worktree_flag": ["--worktree"],
      "extra_flags_seen": ["--no-restore-cwd", "--accept-hooks", "--ignore-user-config", "--ignore-rules", "--skills", "--tui", "--cli", "--usage-file PATH"],
      "supports_json": false,
      "enabled": true,
      "never_use": ["--yolo"],
      "roles": ["browser/runtime specialist", "visual QA", "targeted reproduction", "research needing browser MCP"]
    },
    "opencode": {
      "id": "opencode",
      "command": "/home/lain/.opencode/bin/opencode",
      "command_verified": "ps -eo cmd showed pid 4989 = /home/lain/.opencode/bin/opencode serve --service and pid 19627 = opencode; not on this session's PATH",
      "version": "UNVERIFIED — `opencode --version` not run blind",
      "default_mode": "run",
      "non_interactive_flag": ["run"],
      "subcommand_verified": "opencode run [flags] [<message...>] — 'Run OpenCode with a message'",
      "cwd_flag": "positional [<directory>] on both the top command and `run`",
      "output_format_flag": ["--format", "<default|json>"],
      "output_format_values": "VERIFIED from `opencode run --help`: choices are default, json",
      "permission_flag": ["--auto (auto-approve permissions that are not explicitly denied)"],
      "permission_note": "Without --auto, permissions are NOT auto-approved, which is the safe default for research dispatches. The dispatcher passes --auto only for mode=implement.",
      "tool_restriction_flags": ["--agent <name>"],
      "model_flag": ["-m/--model <provider/model#variant>", "--agent <name>", "--title <string>"],
      "budget_flags": [],
      "supports_resume": true,
      "resume_flags": ["-c/--continue", "-s/--session <id>", "--fork"],
      "supports_stdin": false,
      "prompt_transport": "positional message argument",
      "supports_worktree": false,
      "worktree_flag": "NONE — no worktree flag in --help; use --server/--standalone or an explicit directory instead",
      "extra_flags_seen": ["--standalone", "--server <url>", "--file <path>", "--thinking", "--log-level", "--print-logs"],
      "supports_json": true,
      "enabled": true,
      "never_use": [],
      "roles": ["independent implementation", "alternative implementation", "code review", "second opinion"]
    }
  },
  "coordination": {
    "board": ".agent-pair/BOARD.md",
    "locks_dir": ".agent-pair/locks",
    "heartbeats": ".agent-pair/HEARTBEAT-<worker>.md",
    "inbox": ".agent-pair/inbox/<worker>/<task-id>.json",
    "logs": ".agent-pair/inbox/<worker>/<task-id>.log",
    "active_dispatches": ".agent-pair/dispatch/active/",
    "lock_filename_convention": "<path with / replaced by __>.lock, containing who=, why=, since= (from .agent-pair/README.md rule 1)"
  },
  "beads": {
    "binary": "bd",
    "available_to_this_session": false,
    "evidence": "`command -v bd` fails in both the current and a login shell; `find` over /home/lain, /usr/local, /opt, /usr/lib (maxdepth 4-5) found nothing; .beads/hooks/* guard every call with `command -v bd >/dev/null 2>&1 || skip`, and `.beads/issues.jsonl` does not exist. `.beads/last-touched` was updated at 22:38 by another process, so a Beads runtime is live somewhere on this machine — just not reachable from here.",
    "policy": "The dispatcher probes for bd at dispatch time. If found, it verifies the issue is ready and records the claim. If not found it proceeds in ADVISORY mode, writes beads_status=skipped-unavailable into the result, and never pretends a Beads state change happened."
  },
  "jev": {
    "client": "tools/jev/client.mjs",
    "smoke": "node tools/jev/smoke.mjs",
    "usage_verified": "tools/jev/README.md §Use shows `import { jev, isDecisive } from '../tools/jev/client.mjs'`",
    "secret_policy": "JEV_API_KEY is read from gitignored .env by the client; the dispatcher passes no secrets and never echoes env values"
  }
}
